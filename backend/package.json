{
  "name": "tele-energy",
  "version": "1.0.0",
  "description": "Tele Energy Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd ../frontend && npm install && npm run build",
    "dev": "concurrently \"npm run server\" \"cd frontend && npm run dev\"",
    "server": "node server.js"
  },
  "dependencies": {
    "mqtt": "^5.13.1",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
